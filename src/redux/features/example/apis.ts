// import makeStore from "../../store";
// import { getReq, postReq } from "@utils/request";

// export const getOneAPI = ({ token = makeStore.getStore().auth.token }) =>
//   getReq({ path: "/example", token });

// export const postOneAPI = ({ token = makeStore.getStore().auth.token, data }) =>
//   postReq({ path: "/example", token, data });
